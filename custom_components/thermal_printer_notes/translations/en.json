{
  "title": "Thermal Printer Notes",
  "config": {
    "step": {
      "user": {
        "title": "Set up a thermal printer",
        "description": "Select the ESPHome printer and set its central print and history defaults. Add another entry for every additional printer.",
        "data": {
          "source_device_id": "ESPHome printer",
          "print_action": "ESPHome print action",
          "history_limit": "Maximum history entries per user",
          "copies": "Number of copies",
          "feed_lines": "Feed lines",
          "reverse_print": "Print in reverse",
          "cut": "Cut after printing"
        },
        "data_description": {
          "source_device_id": "The ESPHome device that owns the status, ready, queue, and print action.",
          "print_action": "Service in domain.service format, normally esphome.thermal_printer_print_markdown.",
          "history_limit": "Applies separately to this printer and every user; defaults to 20.",
          "copies": "Used for every job on this printer.",
          "feed_lines": "Paper feed lines before cutting.",
          "reverse_print": "Uses the ESPHome driver's reverse print direction.",
          "cut": "Runs the cutter after the print."
        }
      }
    },
    "error": {
      "invalid_action": "The action must use domain.service format."
    },
    "abort": {
      "already_configured": "This ESPHome printer is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Central print settings",
        "description": "These settings apply to the selected printer. Drafts and history stay separate for each printer and user.",
        "data": {
          "source_device_id": "ESPHome printer",
          "print_action": "ESPHome print action",
          "history_limit": "Maximum history entries per user",
          "copies": "Number of copies",
          "feed_lines": "Feed lines",
          "reverse_print": "Print in reverse",
          "cut": "Cut after printing"
        }
      }
    },
    "error": {
      "invalid_action": "The action must use domain.service format.",
      "already_configured": "This ESPHome printer is already used by another entry."
    }
  }
}
